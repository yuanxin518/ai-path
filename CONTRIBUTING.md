# 如何维护这个清单

这个仓库的核心模式是：**你说你用了什么工具，我帮你归到对应的节点，加到流程图里。**

---

## 工作流程

你引入一个工具 → 我判断它属于哪个阶段 → 放入对应节点 → 更新 Mermaid 流程图

当前已定义的阶段节点：

| 阶段 | 说明 | 例子 |
|------|------|------|
| **大模型** | 底层 AI 能力 | DeepSeek V4, Claude, GPT |
| **终端** | 交互界面 | DeepSeek TUI, Claude Code |
| **其他（等你引入）** | 你说了算 | Agent, 插件, 知识库, 工作流…… |

> 阶段名称不是固定的，你引入的工具决定节点应该叫什么、放在什么位置。

---

## 如何添加一个工具

**步骤一**：告诉 AI「我用了 xxx 工具」

```
我用了 deepseek-v4-flash
```

**步骤二**：AI 会判断这个工具属于哪个阶段，然后在对应节点的目录下更新 README：

```
大模型/
├── README.md    ← AI 在这加一行：- DeepSeek V4 Flash
```

**步骤三**：AI 更新根 README 的 Mermaid 流程图，把新工具展示到对应节点框内。

---

## 目录结构

```
ai-path/
├── README.md          ← 主地图（Mermaid 流程图 + 工具清单）
├── 大模型/             ← 每个阶段一个目录
│   └── README.md
├── 终端/
│   └── README.md
└── (更多节点目录…)
```

---

## 要点

- **不预置空节点** — 你没有引入的工具不会出现在仓库里
- **竖向流程图** — 从上到下展示链路顺序
- **目录名即阶段名** — 阶段名用中文，读的人一眼明白
- **每个人都可以按这个模式改** — 引入你自己的工具，AI 会自动重组
