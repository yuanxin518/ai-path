# 节点构建规范

> 本文档总结了 ai-path 技术链路地图的节点构建规则、连接规范和样式约定。

---

## 1. 节点结构

### 1.1 目录规范

```
节点名/
└── README.md    ← 该节点内工具的详细说明
```

- 节点目录名即阶段名，用**中文**
- 根 README 的流程图和表格中通过 `[节点名](./节点名/)` 链接
- 工具细节写在目录内的 README.md 中

### 1.2 节点内展示

每个节点在 Mermaid 流程图中以 **子图（subgraph）** 形式展示内部工具：

```mermaid
subgraph N00["节点名"]
    direction TB
    TOOL1["工具 1"]
    TOOL2["工具 2 ⭐"]    ← ⭐ 表示重点/常用
end
```

### 1.3 节点命名

- 使用 **emoji + 中文名** 格式（如 `🖥 操作系统`、`⌨️ 终端`）
- emoji 帮助快速识别类型，保持一致性

| 类型 | Emoji |
|------|-------|
| 操作系统 | 🖥 |
| 终端 | ⌨️ |
| 大模型/AI | 🤖 |
| 路由/网络 | 🔀 |
| 技能 | 🎯 |
| 工具 | 🔧 |
| 提示词 | 💬 |
| 浏览器 | 🌐 |
| 知识库 | 📚 |
| 阅读 | 📰 |

---

## 2. 连接规则

### 2.1 中心节点

- **「我 ⭐」** 是根节点，作为所有链路的起点
- 样式：黄色高亮 `#fff3cd` 背景 + `#f59e0b` 橙色边框，粗 3px

### 2.2 出度（输出）规则

```
尽量让每个节点只有 1 个出（→）
最多不超过 2 个出
```

- ✅ 好的例子：`我 → 操作系统`（1 出）
- ⚠️ 可接受：`操作系统 → 终端 / → 浏览器`（2 出，OS 自然分叉）
- ❌ 避免：一个节点指向 3 个以上其他节点

### 2.3 入度（输入）规则

```
可以有多个入（多个节点指向同一个节点）
```

- ✅ 多个上游节点汇聚到同一个下游节点是允许的
- 这表示该节点承接多个来源的关联

### 2.4 连接语义

| 箭头 | 含义 |
|------|------|
| `-->` | 强关联（A 依赖 B、A 驱动 B） |
| `-.->` | 弱关联（运行在...之上、可选链路） |

连线应反映**实际技术关系**，而非随意排列。

---

## 3. 配色规范

### 3.1 主色调

```
统一使用中性灰阶，只有重点才使用彩色：
```

| 元素 | 填充 | 边框 | 用途 |
|------|------|------|------|
| 「我 ⭐」 | `#fff3cd` | `#f59e0b` | 🔶 **唯一主高亮** |
| 操作系统 | `#e8f5e9` | `#66bb6a` | 🟢 绿色（基础环境） |
| 浏览器 | `#e3f2fd` | `#42a5f5` | 🔵 蓝色 |
| 知识库 | `#fce4ec` | `#ef5350` | 🫚 粉色 |
| 阅读 | `#f3e5f5` | `#ab47bc` | 🟣 紫色 |
| 普通节点 | `#ffffff` | `#cccccc` | ⚪ 中性灰白 |
| 重点工具 ⭐ | `#fff3cd` | `#f59e0b` | 🔶 黄色（仅高亮特别重要的工具，如 Reasonix Code） |

### 3.2 高亮原则

- **只有「我 ⭐」** 在主图中使用高亮
- 子图内可对**特别重要的工具**单独高亮（如 Reasonix Code ⭐）
- 不得滥用彩色，保持整体视觉统一

### 3.3 子图边框

子图（subgraph）的边框颜色和填充与其主题一致，内部工具节点使用纯白背景。

---

## 4. 操作系统三层架构

操作系统节点展示三层结构，使用虚线箭头表示层级关系：

```
┌─ 🖥 操作系统 ───────────────┐
│ 🪟 Windows（宿主机）         │  ← 浅绿背景
│     │  -.-→                  │
│ ⚡ WSL 2（虚拟化层）🟡      │  ← 黄色高亮（中间层）
│     │  -.-→                  │
│ 🐧 openSUSE Tumbleweed       │  ← 浅绿背景
└──────────────────────────────┘
```

- **Windows** 是宿主机
- **WSL 2** 是虚拟化中间层（黄色高亮标记）
- **openSUSE Tumbleweed** 是 Linux 开发环境

---

## 5. 表格规范

根 README 的表格格式：

```markdown
| 节点 | 工具 |
|------|------|
| [🖥 操作系统](./操作系统/) | 🪟 **Windows** → ⚡ **WSL 2** → 🐧 **openSUSE Tumbleweed** |
| [⌨️ 终端](./终端/) | 工具1 · ⭐ 工具2 · [工具3](https://github.com/...) |
```

- **有 GitHub 仓库的工具** → 标题改为 Markdown 超链接：`[工具名](https://github.com/...)`
- 工具之间用 `·` 分隔
- 重点工具前加 ⭐

---

## 6. 链路图布局

- 使用 `flowchart TD`（从上到下）
- 节点编号按 N00, N01, N02... 递增
- 不需要严格按上下顺序排列，重要的是**保留关联关系**
- 避免形成环路（A→B→C→A）

---

## 7. 工具添加流程

1. 在对应节点目录的 README.md 中添加工具条目
2. 在根 README 的 Mermaid 子图中添加工具节点
3. 在根 README 的表格中添加或更新该行
4. 如有 GitHub 仓库，添加超链接
5. 如有仓库详情需要补充，更新节点目录下的 repos.md 或 README.md
